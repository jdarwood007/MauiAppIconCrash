<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:root="clr-namespace:MauiApp6"
            x:DataType="root:AddViewModel"
             x:Class="MauiApp6.AddPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Notes" IconImageSource="{Binding NoteIcon, FallbackValue=button_empty}" />
        <ToolbarItem Text="Test"></ToolbarItem>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label
                Text="Add Page"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
